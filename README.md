# truman-cho.com

Static rebuild of Truman's portfolio site, previously on Squarespace.
Plain HTML/CSS, no build step. Generated from `build_site.py` (source content in `_data/`, gitignored).

Deployed via Cloudflare Pages, same pattern as betrumaker.com (see ~/Developer/MakerSite).
